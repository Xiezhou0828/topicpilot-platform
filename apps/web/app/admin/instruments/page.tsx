"use client"; /* eslint-disable @typescript-eslint/no-explicit-any */
import Link from "next/link"; import { useEffect, useState } from "react"; import { AdminNav } from "../AdminNav";
const API=(process.env.NEXT_PUBLIC_API_BASE_URL||"http://localhost:8000").replace(/\/$/,"");
export default function Instruments(){const [q,setQ]=useState("");const [data,setData]=useState<any>();useEffect(()=>{fetch(`${API}/api/v1/admin/instruments?q=${encodeURIComponent(q)}`).then(r=>r.json()).then(setData)},[q]);return <main className="adminShell"><AdminNav/><p className="eyebrow">IDENTITY EXPLORER</p><h1>Instruments</h1><input className="adminInput" placeholder="Search code or name" value={q} onChange={e=>setQ(e.target.value)}/><div className="adminTable">{data?.items?.map((x:any)=><Link className="adminRow" href={`/admin/instruments/${x.id}`} key={x.id}><b>{x.code}</b><span>{x.name||"—"}</span><span>{x.instrument_type} · {x.is_active?"ACTIVE":"INACTIVE"}</span></Link>)}</div></main>}
