"use client"; /* eslint-disable @typescript-eslint/no-explicit-any */
import Link from "next/link"; import {useEffect,useState} from "react"; import {AdminNav} from "../AdminNav"; const API=(process.env.NEXT_PUBLIC_API_BASE_URL||"http://localhost:8000").replace(/\/$/,"");
export default function Topics(){const[q,setQ]=useState("");const[d,setD]=useState<any>();useEffect(()=>{fetch(`${API}/api/v1/admin/topics?q=${encodeURIComponent(q)}`).then(r=>r.json()).then(setD)},[q]);return <main className="adminShell"><AdminNav/><p className="eyebrow">TOPIC EXPLORER</p><h1>Topics</h1><input className="adminInput" placeholder="Search slug or name" value={q} onChange={e=>setQ(e.target.value)}/><div className="adminTable">{d?.items?.map((x:any)=><Link className="adminRow" href={`/admin/topics/${x.id}`} key={x.id}><b>{x.slug}</b><span>{x.name}</span><span>{x.status}</span></Link>)}</div></main>}
